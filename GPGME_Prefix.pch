//
// Prefix header for all source files of the 'GPGME' target in the 'GPGME' project.
//

// We can't simply use <gpgme.h>, because due to HFS' case-insensitivity and the fact that we have a GPGME.h file,
// compiler doesn't make difference between both files!

#include "/usr/local/include/gpgme.h"

#ifdef __OBJC__
#import <Foundation/Foundation.h>
#endif
